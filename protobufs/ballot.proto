syntax = "proto3";

package ringvote;

import "response.proto";

message Ballot {
    optional string poll_title = 1;
    repeated ringvote.Response responses = 2;
    optional bytes signature = 3;
}
