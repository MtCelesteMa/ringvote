syntax = "proto3";

package ringvote;

message Voter {
    message ExtraInfo {
        optional string title = 1;
        optional string content = 2;
    }

    optional string name = 1;
    repeated ExtraInfo extra_infos = 2;
    optional bytes public_key = 3;
}
